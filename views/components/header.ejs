<header class="unselectable">
    <div class="left">
        <a href="/">Bast</a>
    </div>
    <div class="middle">
        <%- include('./search-bar.ejs') %>
    </div>
    <div class="right">
        <% if (locals.auth) { %>
            <a href="/logout">Logout</a>
            <a href="/profile"><%= locals.auth.name %></a>
        <% } else { %>
            <a href="/signup">Sign up</a>
            <a href="/login">Login</a>
        <% } %>
    </div>
</header>